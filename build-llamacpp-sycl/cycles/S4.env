# S4: pure GPU — tensor split across same-GPU tiles (still -ngl 99)
export ZE_FLAT_DEVICE_HIERARCHY=FLAT
export ZE_AFFINITY_MASK=0,1
export SPLIT_MODE=tensor
export N_CTX=2048
export NGL=99
export EXE=/lus/flare/projects/MOFA/xiaoliyan/workdir/llm/gpt-oss-120b/build-llamacpp-sycl/build/bin/llama-completion
export EXTRA_ARGS='--no-mmap -v -fa on'
