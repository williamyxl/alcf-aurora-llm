# P4: FLAT layer + VMM=0 + PVC IGC/GRF knobs (bench-only)
export ZE_FLAT_DEVICE_HIERARCHY=FLAT
export ZE_AFFINITY_MASK=0,1
export SPLIT_MODE=layer
export NGL=99
export N_PREDICT=128
export GGML_SYCL_ENABLE_VMM=0
# RUN_CLI obsolete — MEASURE_TTFT=1 always in perf PBS
export MEASURE_TTFT=1
export RUN_BENCH=1
export EXTRA_ARGS='-fa on'
# PVC tuning (safe no-ops if ignored)
export IGC_EnableGEPLRUCache=1
export IGC_ForceOCLSIMDWidth=16
export NEO_CACHE_PERSISTENT=1
